<UserControl x:Class="GymAdminPanel.Views.TrainersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F4F6F9">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Trenerzy"
                       FontSize="28" FontWeight="Light" Foreground="#2C3E50"/>
            <TextBlock Text="{Binding StatusText}"
                       FontSize="13" Foreground="#7F8C8D" Margin="0,4,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="10,8" FontSize="14" Margin="0,0,8,0"
                     Background="White" BorderBrush="#DDE1E7" BorderThickness="1"
                     VerticalContentAlignment="Center">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="🔍  Szukaj po imieniu, nazwisku lub specjalizacji..."
                                                           Foreground="#AAB0B8" FontSize="14" Margin="10,0,0,0"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <Button Grid.Column="1" Content="🔄 Odśwież"
                    Command="{Binding LoadTrainersCommand}"
                    Padding="14,8" Background="#2980B9" Foreground="White"
                    FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" FontSize="13"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition>
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="0"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditPanelVisible}" Value="True">
                                    <Setter Property="Width" Value="320"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White"
                    BorderBrush="#DDE1E7" BorderThickness="1" CornerRadius="4"
                    Margin="0,0,8,0">
                <Grid>
                    <Grid Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                          Background="#88FFFFFF" Panel.ZIndex="10">
                        <TextBlock Text="⏳ Ładowanie..."
                                   FontSize="16" Foreground="#2C3E50"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding FilteredTrainers}" Margin="12">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="220" Margin="6"
                                            Background="White"
                                            BorderBrush="#DDE1E7" BorderThickness="1"
                                            CornerRadius="8"
                                            Cursor="Hand">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="BorderBrush" Value="#2980B9"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#2980B9"
                                                                                  BlurRadius="8"
                                                                                  Opacity="0.2"
                                                                                  ShadowDepth="0"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel Margin="16">
                                            <Border Width="64" Height="64"
                                                    CornerRadius="32"
                                                    Background="#2C3E50"
                                                    HorizontalAlignment="Center"
                                                    Margin="0,0,0,12">
                                                <TextBlock HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Foreground="White"
                                                           FontSize="22" FontWeight="Bold">
                                                    <TextBlock.Text>
                                                        <MultiBinding StringFormat="{}{0}{1}">
                                                            <Binding Path="FirstName[0]"/>
                                                            <Binding Path="LastName[0]"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </Border>

                                            <TextBlock HorizontalAlignment="Center"
                                                       FontSize="15" FontWeight="SemiBold"
                                                       Foreground="#2C3E50"
                                                       TextAlignment="Center"
                                                       Margin="0,0,0,4">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} {1}">
                                                        <Binding Path="FirstName"/>
                                                        <Binding Path="LastName"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <Border CornerRadius="3" Padding="8,3"
                                                    Background="#EBF5FB"
                                                    HorizontalAlignment="Center"
                                                    Margin="0,0,0,10">
                                                <TextBlock Text="{Binding Specialization}"
                                                           FontSize="11" FontWeight="SemiBold"
                                                           Foreground="#2980B9"
                                                           TextAlignment="Center"/>
                                            </Border>

                                            <TextBlock Text="{Binding Bio}"
                                                       FontSize="12" Foreground="#7F8C8D"
                                                       TextWrapping="Wrap"
                                                       MaxHeight="50"
                                                       TextAlignment="Center"
                                                       Margin="0,0,0,12"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <Button Content="✏ Edytuj profil"
                                                    Command="{Binding DataContext.OpenEditCommand,
                                                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Background="#2980B9" Foreground="White"
                                                    BorderThickness="0" Padding="0,7"
                                                    FontSize="12" Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#1A6FA8"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="1"
                    Visibility="{Binding IsEditPanelVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                    Background="White" BorderBrush="#DDE1E7" BorderThickness="1"
                    CornerRadius="4" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Edytuj profil trenera"
                                   FontSize="16" FontWeight="SemiBold"
                                   Foreground="#2C3E50" Margin="0,0,0,20"/>

                        <TextBlock Text="Imię *" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding EditFirstName, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,7" FontSize="13" BorderBrush="#DDE1E7"
                                 Margin="0,0,0,12"/>

                        <TextBlock Text="Nazwisko *" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding EditLastName, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,7" FontSize="13" BorderBrush="#DDE1E7"
                                 Margin="0,0,0,12"/>

                        <TextBlock Text="Specjalizacja" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding EditSpecialization, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,7" FontSize="13" BorderBrush="#DDE1E7"
                                 Margin="0,0,0,12"/>

                        <TextBlock Text="Bio" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding EditBio, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,7" FontSize="13" BorderBrush="#DDE1E7"
                                 Height="90" TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,12"/>

                        <TextBlock Text="URL zdjęcia" FontSize="11" Foreground="#7F8C8D" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding EditPhotoUrl, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,7" FontSize="13" BorderBrush="#DDE1E7"
                                 Margin="0,0,0,20"/>

                        <Button Content="✓ Zapisz zmiany"
                                Command="{Binding SaveEditCommand}"
                                Padding="0,10" Margin="0,0,0,8"
                                Background="#27AE60" Foreground="White"
                                BorderThickness="0" Cursor="Hand" FontSize="13">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="✕ Anuluj"
                                Command="{Binding CancelEditCommand}"
                                Padding="0,10"
                                Background="#95A5A6" Foreground="White"
                                BorderThickness="0" Cursor="Hand" FontSize="13">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <TextBlock Grid.Row="3"
                   Text="{Binding FooterText}"
                   FontSize="12" Foreground="#95A5A6" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
