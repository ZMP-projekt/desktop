name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    name: Build and test
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore
        run: dotnet restore GymAdminPanel/GymAdminPanel.sln

      - name: Build
        run: dotnet build GymAdminPanel/GymAdminPanel.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test GymAdminPanel/GymAdminPanel.Tests/GymAdminPanel.Tests.csproj --no-build --configuration Release --verbosity minimal
